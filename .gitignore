# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# The .xcodeproj is generated by XcodeGen from project.yml, but committed so
# the repo can be opened in Xcode without running xcodegen. User-specific
# state inside it is ignored above.

# macOS
.DS_Store

# SwiftPM (legacy, if anyone runs swift build)
.build/
.swiftpm/
Package.resolved
